Для оптимизации бд я бы добавил индексы на столбцы: categories.parent_id(для 2.2),
products.category_id(для ускорения связи товаров с категориями), orders.client_id(2.1),
orders.created_at(2.3.1) и order_items.product_id(2.3.1). Также я уже создал составной индекс на 
order_items.order_id и order_items.product_id, который сильно увеличивает производительность. 
И если в будущем будет очень сильная нагрузка, то можно было бы еще применить репликацию